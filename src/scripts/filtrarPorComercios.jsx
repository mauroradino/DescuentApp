import getProductos from "./getProductos"

const filtrarPorComercios = (comercio) => {
    let productos = getProductos()
   /* let productosFiltrados = productos.filter((producto) => producto.comercio === comercio)*/
//    return productosFiltrados
}

export default filtrarPorComercios
